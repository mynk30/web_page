1. user me profile defult set karni hai ya check kar lena ek bar 
ALTER TABLE `users` CHANGE `reset_otp` `otp` INT(4) NULL DEFAULT NULL, CHANGE `reset_otp_expiry` `otp_expiry` DATETIME NULL DEFAULT NULL;


2. form me QR code add karna payment 
3. admin document upload karega application comlete hone ke bad certificat vo user ke es me show hona chaiye 


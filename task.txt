1. user me profile defult set karni hai ya check kar lena ek bar 
ALTER TABLE `users` CHANGE `reset_otp` `otp` INT(4) NULL DEFAULT NULL, CHANGE `reset_otp_expiry` `otp_expiry` DATETIME NULL DEFAULT NULL;


2. form me QR code add karna payment 
3. admin document upload karega application comlete hone ke bad certificat vo user ke es me show hona chaiye 

ALTER TABLE applications ADD UNIQUE (application_number);

CREATE TABLE orders (
    id INT NOT NULL AUTO_INCREMENT,
    order_no VARCHAR(50) NOT NULL UNIQUE,
    amount DECIMAL(10,2) NOT NULL,
    payment_status ENUM('paid', 'unpaid') NOT NULL DEFAULT 'unpaid',
    currency VARCHAR(10) NOT NULL,
    order_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    customer_details JSON NOT NULL,
    application_number VARCHAR(50) NOT NULL,
    PRIMARY KEY (id),
    FOREIGN KEY (application_number) REFERENCES applications(application_number)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);

ALTER TABLE orders 
ADD COLUMN payment_id VARCHAR(100) NULL AFTER currency,
ADD COLUMN order_id VARCHAR(100) NULL AFTER payment_id,
ADD COLUMN signature VARCHAR(255) NULL AFTER order_id,
ADD COLUMN paid_at DATETIME NULL AFTER signature;


ALTER TABLE orders 
ADD COLUMN user_id INT(11) NOT NULL AFTER order_no,
ADD COLUMN service_type VARCHAR(100) NOT NULL AFTER user_id;
